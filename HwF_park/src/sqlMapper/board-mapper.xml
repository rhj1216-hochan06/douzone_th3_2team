<?xml version="1.0" encoding="UTF-8"?>
<!-- boardmapper.xml DML 명령 처리 -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dao">

	<select id="selectAll" resultType="dto">
		select * from QnA order by qnaid desc
	</select>

	<insert id="insert" parameterType="dto">
		insert into QnA(qnaId,
		qnadate, qnacontent, qnaanswer, memberid)
		values(qna_id_seq.nextval, sysdate , #{qnacontent} , #{qnaanswer}, #{memberid})
	</insert>

	<select id="selectSearch" parameterType="hashMap"
		resultType="dto">

		SELECT * FROM qna
		<!-- where writer like '%김연아%' -->
		<if test="column != null">
			where ${column} like '%' || #{keyvalue} || '%' order by qnaid desc
		</if>
	</select>

	 
	 <select id="searching" parameterType="hashMap" 
	 	resultType="search">
	 	
	<if test="column != null">
		select * from Healthgoods where  ${column} like '%' || #{keyvalue} || '%'  order by Healthgoodsid desc;
	</if>
	</select>


</mapper>